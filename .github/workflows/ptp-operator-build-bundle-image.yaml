name: "ptp-operator: Deploy Bundle Images to GHCR"
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  packages: write
  contents: read

jobs:
  build-bundle:
    uses: edcdavid-org/ptp-operator-upstream-monorepo/.github/workflows/_reusable-ptp-operator-build-bundle-image.yml@main
    secrets: inherit
