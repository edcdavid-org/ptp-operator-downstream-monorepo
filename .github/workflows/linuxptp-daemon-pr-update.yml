name: "linuxptp-daemon: Run Jobs on PR"
on:
  pull_request:
    paths:
      - 'pkg/linuxptp-daemon/**'

jobs:
  pr-update:
    uses: edcdavid-org/ptp-operator-upstream-monorepo/.github/workflows/_reusable-linuxptp-daemon-pr-update.yml@main
    secrets: inherit
