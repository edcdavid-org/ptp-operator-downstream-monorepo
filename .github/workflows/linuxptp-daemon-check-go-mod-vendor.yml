name: "linuxptp-daemon: Go mod tidy and vendor"
on:
  push:
    branches: ["main", "one-repo*", "release-*"]
    paths:
      - 'pkg/linuxptp-daemon/**'
  pull_request:
    paths:
      - 'pkg/linuxptp-daemon/**'

jobs:
  check:
    uses: edcdavid-org/ptp-operator-upstream-monorepo/.github/workflows/_reusable-linuxptp-daemon-check-go-mod-vendor.yml@main