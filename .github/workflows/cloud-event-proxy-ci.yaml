name: "cloud-event-proxy: CI"
on:
  push:
    paths:
      - 'pkg/cloud-event-proxy/**'
  pull_request:
    paths:
      - 'pkg/cloud-event-proxy/**'

jobs:
  ci:
    uses: edcdavid-org/ptp-operator-upstream-monorepo/.github/workflows/_reusable-cloud-event-proxy-ci.yml@main