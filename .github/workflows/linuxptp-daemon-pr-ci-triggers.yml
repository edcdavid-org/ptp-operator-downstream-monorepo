name: "linuxptp-daemon: PR CI Triggers"
on:
  pull_request_target:
    types: [opened, reopened]
    paths:
      - 'pkg/linuxptp-daemon/**'

jobs:
  triggers:
    uses: edcdavid-org/ptp-operator-upstream-monorepo/.github/workflows/_reusable-linuxptp-daemon-pr-ci-triggers.yml@main
    secrets: inherit
